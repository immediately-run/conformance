// The SDK's ambient `fs` module types (the sandbox's async-only filesystem).
// https://opencode.ai — activates `import fs from 'fs'` type-checking in the
// browser project without pulling in @types/node.
/// <reference types="@immediately-run/sdk/ambient" />
